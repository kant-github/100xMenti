import { useLiveSessionStore } from "@/zustand/liveSession"

export default function HostLiveRightPanel() {
    const { liveSession } = useLiveSessionStore();
    return (
        <div className="grid">

        </div>
    )
}